# Reference book (not for distribution)
tdd-buildautomation-ci.pdf
AST\ Exam.pdf

# Personal notes — keep README only
*.md
!README.md

# Local working instructions
instructions.txt

# Maven build output — must NOT be in git (exam requirement)
target/
bin/
*.class

# OS
.DS_Store

# Eclipse metadata must be tracked — do not add .settings/, .project, .classpath here

# Environment variables and credentials (contains secrets)
.env
CREDENTIALS.txt
