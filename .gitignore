
# Markdown notes & AI assistant files — keep README only
*.md
!README.md

# Local working instructions (contains AI-related notes)
instructions.txt

# Maven build output — must NOT be in git (exam requirement)
target/
bin/
*.class

# OS
.DS_Store

# NOTE: .settings/, .project, .classpath must NOT be ignored
# The exam PDF requires all Eclipse metadata in the git repo

# Environment variables and credentials (contains secrets)
.env
CREDENTIALS.txt
